const { wallpaper } = require("../../lib/scrape");module.exports = {    name: ["wp"],    type: ["misc"],    description: "searching wallpaper image",    utilisation: global.userbot.prefix+ "wp <tema>",        async execute(m) {        let {conn, text} = data        if (!text) return m.reply(`contoh: ${userbot.prefix}wp aesthetic`)        try {            const json = await wallpaper(text)            conn.sendFile(m.chat, json[Math.floor(Math.random () * json.length)], "", null, m)        } catch (e) {            console.log(e)            m.reply("Gagal mengirim gambar!")        }    }}